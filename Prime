#include <stdio.h>
int main(){
int n,i,j,isprime;
printf("enter the value of n: ");
scanf("%d",&n);
printf("prime nums upto %d are:\n",n);
for(i=2;i<=n;i++){
isprime=1;
for(j=2;j<i;j++){
if(i%j==0){
isprime=0;
break;
}
}
if(isprime==1){
printf("%d",i);
}}
return 0;
}

