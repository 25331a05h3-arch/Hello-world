#include <stdio.h>


int sum(int a, int b) {
    return a + b;
}

int main() {
    int n1, n2, total;
    printf("Enter two integers: ");
    scanf("%d %d", &n1, &n2);

       total = sum(n1, n2);
    printf("Sum = %d\n", total);
    return 0;
}

